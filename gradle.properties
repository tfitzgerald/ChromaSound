# Android build properties
android.useAndroidX=true
android.enableJetifier=true

# Gradle JVM memory — prevents OOM on GitHub's runners
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Kotlin code style
kotlin.code.style=official
